id: lmstudio
name: LMStudio
type: local
endpoint: http://127.0.0.1:1234/v1
api_key: ${LMSTUDIO_API_KEY}
# context_window pins the context window (in tokens) for models served by
# this provider, for servers that don't report one. LM Studio reports the
# loaded context length itself, so this is normally left unset.
# context_window: 8192
