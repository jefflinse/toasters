Emit many small, focused tasks.

Target 5–10 tasks. Each task should target a single specific outcome
— one handler, one schema change, one piece of middleware, one test
file. Split implementation from verification. Keep each task's scope
tight enough that a worker can complete it in one bounded pass.
