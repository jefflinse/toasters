Target small, focused units of work per executable piece.

Prefer specialized graphs over generic ones when the task's shape is
clear. Reject and subdivide whenever a task holds more than one
coherent piece of work. When subdividing, emit several small subtasks
— each one should correspond to a single specific outcome, not a
bundle of related outcomes.
