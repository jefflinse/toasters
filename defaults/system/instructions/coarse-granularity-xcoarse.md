Emit very few, very large tasks.

Target 1–3 tasks total. Each one should cover a broad slice of the job
— ideally an entire deliverable end-to-end. Bundle related concerns
(data model + API + tests) into a single task rather than splitting
them. Only split when two pieces of work genuinely cannot share a
single run.
