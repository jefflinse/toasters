Emit the smallest viable tasks.

Split aggressively. Each task should target exactly one atomic
outcome: one function, one endpoint handler, one config key, one
test case. Never bundle multiple concerns. If you find yourself writing
"and" in a task title, split it. 10+ tasks is normal at this level,
and foundational dependencies should be individually named.
