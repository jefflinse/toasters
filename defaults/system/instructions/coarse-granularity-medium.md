Emit a moderate number of moderately-sized tasks.

Target 3–6 tasks. Each task should cover one logical concern end-to-end
(e.g. a single endpoint with its validation and tests, or one data
model change with its migration). Separate verification from
implementation when the verification is non-trivial.
