Each task must be specific and self-contained. A good task description gives the assigned team enough context to start without reading the full job description. "Add a `created_at` column to the users table" not "Update the database." If a task feels too large, split it. Order tasks so foundational work comes first.
