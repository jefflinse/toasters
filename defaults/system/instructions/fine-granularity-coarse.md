Target large units of work per executable piece.

Prefer broader graphs when a good fit exists. Reject and subdivide only
when a task clearly spans more than one graph's worth of scope. When
you do subdivide, emit a small number of sizeable subtasks — each one
should represent a substantial slice of the original task.
