Emit a small number of large tasks.

Target 2–4 tasks. Each task should span a meaningful chunk of the job
(a whole subsystem, a full feature, or an end-to-end vertical slice).
Prefer bundling tightly-related work over splitting it. Verification
may live inside the implementing task rather than as a separate one.
